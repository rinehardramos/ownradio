# Local development defaults — Next.js loads this automatically in dev mode.
# These are NOT used in production builds (NEXT_PUBLIC_* must be set in Vercel).
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=http://localhost:4000
